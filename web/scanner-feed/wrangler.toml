name = "scanner-feed"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Create a KV namespace in the Cloudflare dashboard, then paste its ID here:
# Dashboard → Workers & Pages → KV → Create a namespace → copy the ID
kv_namespaces = [
  { binding = "SCANNER_KV", id = "773238e18e4c46408033b70a129b896b" }
]

# Set the ingest token as a secret (not in this file):
#   npx wrangler secret put INGEST_TOKEN
# Then paste any random string as the value.
